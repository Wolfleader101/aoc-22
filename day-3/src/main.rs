fn main() {
    //read file
    let rucksacks: i32 = fs::read_to_string("input.txt")
    //split on new line
    // split string in half

        // loop over each character in string 1 and check if it is in string 2
        // get the priority from the hashmap for it
        // add it to the vector
    // sum the vector
}
